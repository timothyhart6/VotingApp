@model CivicsApp.Models.Representative

@{
    ViewData["Title"] = "Representatives";
}

<h2>List of Representatives</h2>

<p>This is coming from the Representatives View/Index.cshtml</p>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>

</thead>

    <div>
        <h4>Testing Database connection(expect last name = "Johnson"):</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.LastName)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.LastName)
            </dd>
        </dl>
    </div>

    @*<ul>
            @for (int i = 0; i < (int)ViewData["NumTimes"]; i++)
            {
                <li>@ViewData["Message"]</li>
            }
        </ul>*@
    @*<tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.LastName)
                        </td>
                    </tr>
        }
            </tbody>*@
</table>