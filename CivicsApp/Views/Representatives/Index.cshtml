@model List<CivicsApp.Models.Representatives.CompleteListOfReps.Representative>

@{
    ViewData["Title"] = "Representative";
}

<h2>List of Representatives</h2>


<table class="table">

    <thead>
        <tr>
            @*<th>
                @Html.DisplayNameFor(model => model.Results[0].Members[0].State)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Results[0].Members[0].District)
            </th>*@
            <th>
                @Html.DisplayNameFor(model => model[0].FirstName)
            </th>
            @*<th>
                @Html.DisplayNameFor(model => model.Results[0].Members[0].LastName)
            </th>*@
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                @*<td>
                    @Html.DisplayFor(modelItem => item.State)
                </td>
                <td>
                    @Html.DisplayFor(ModelItem => item.District)
                </td>*@
                <td>
                    @Html.DisplayFor(Model => item.FirstName)
                </td>
                @*<td>
                    @Html.DisplayFor(ModelItem => item.LastName)
                </td>*@
            </tr>
        }
    </tbody>
</table>